import { EmptyState } from "../components/EmptyState";

export function ConfigHelperScreen() {
  return (
    <EmptyState
      title="Config Helper"
      description="Hermes and Codex apply flows remain in the legacy UI until the React API client and form states are migrated safely."
    />
  );
}
