import { EmptyState } from "../components/EmptyState";

export function AuthScreen() {
  return (
    <EmptyState
      title="Auth"
      description="Account auth management is intentionally not migrated yet. This route is reserved for the future React screen."
    />
  );
}
