import { EmptyState } from "../components/EmptyState";

export function ClientsScreen() {
  return (
    <EmptyState
      title="Clients"
      description="Client CRUD will be migrated in a later phase. Existing client forms remain in the legacy dashboard."
    />
  );
}
