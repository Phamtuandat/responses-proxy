import { EmptyState } from "../components/EmptyState";

export function RtkScreen() {
  return (
    <EmptyState
      title="RTK"
      description="RTK policy editing stays in the legacy UI until form migration begins."
    />
  );
}
