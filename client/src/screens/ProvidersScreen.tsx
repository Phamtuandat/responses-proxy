import { EmptyState } from "../components/EmptyState";

export function ProvidersScreen() {
  return (
    <EmptyState
      title="Providers"
      description="Provider CRUD will be migrated in a later phase. This shell keeps the route and layout ready without adding mutations."
    />
  );
}
